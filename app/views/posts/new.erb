<%= form_for @post do |p| %>
	Post Title: <%= p.text_field :title %></br>
	Content: <%= p.text_area :content %></br>
	Select your post's categories! <%= p.collection_check_boxes(:category_ids, @categories, :id, :name)%></br>
	<%= p.submit "Create Post" %>
<% end %>
